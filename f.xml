<?xml version="1.0" encoding="UTF-8" ?>
<Module>
    <ModulePrefs title="mc" />
    <Content type="html"><![CDATA[
<body>
   <script>
document.addEventListener("DOMContentLoaded", () => {
    const mcurl = "https://cdn.jsdelivr.net/gh/genizy/google/mc.html";
    fetch(mcurl)
        .then(response => {
            if (!response.ok) {
                throw new Error('Network response was not ok: ' + response.statusText);
            }
            return response.text();
        })
        .then(html => {
            // Create a wrapper for the fetched HTML
            const wrapper = document.createElement("div");
            wrapper.innerHTML = html;
            document.body.appendChild(wrapper);

            // Find all script tags in the fetched HTML and execute them
            const scripts = wrapper.querySelectorAll("script");
            scripts.forEach(oldScript => {
                const newScript = document.createElement("script");
                if (oldScript.src) {
                    newScript.src = oldScript.src; // Load external scripts
                    newScript.onload = () => console.log(`Loaded script: ${oldScript.src}`);
                } else {
                    newScript.textContent = oldScript.textContent; // Inline scripts
                }
                document.body.appendChild(newScript); // Append the script to the body
            });
        })
        .catch(error => {
            console.error('Error fetching the HTML code:', error.message);
        });
});

   </script>
</body>
    ]]></Content>
</Module>
